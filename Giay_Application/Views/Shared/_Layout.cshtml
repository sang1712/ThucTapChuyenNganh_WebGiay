<!DOCTYPE html>
<html lang="vi">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>@ViewData["Title"] - SneakerStore</title>
    <link rel="stylesheet" href="~/lib/bootstrap/dist/css/bootstrap.min.css" />
    <link rel="stylesheet" href="~/css/site.css" asp-append-version="true" />
    <link rel="stylesheet" href="~/WebApplication1.styles.css" asp-append-version="true" />
    <style>
        body {
            font-family: 'Poppins', sans-serif;
            background-color: #f8f9fa;
        }

        /* ====== Navbar ====== */
        .navbar {
            background: linear-gradient(90deg, #000000, #212529);
            box-shadow: 0 3px 10px rgba(0, 0, 0, 0.3);
        }

        .navbar-brand, .nav-link {
            color: #fff !important;
            font-weight: 500;
        }

            .navbar-brand:hover, .nav-link:hover {
                color: #0d6efd !important;
            }

        /* ====== Banner ====== */
        .hero {
            position: relative;
            background: url('/images/panner.jpg') center/cover no-repeat;
            color: white;
            text-align: center;
            padding: 150px 20px;
            border-radius: 10px;
            margin-bottom: 50px;
            overflow: hidden;
        }

            .hero::after {
                content: "";
                position: absolute;
                inset: 0;
                background: rgba(0, 0, 0, 0.5);
            }

        .hero-content {
            position: relative;
            z-index: 2;
        }

        .hero h1 {
            font-size: 3.5rem;
            font-weight: 700;
            text-shadow: 2px 2px 10px #000;
        }

        .hero p {
            font-size: 1.25rem;
            opacity: 0.9;
        }

        .btn-shop {
            background-color: #0d6efd;
            border: none;
            color: #fff;
            font-size: 1.1rem;
            padding: 10px 25px;
            border-radius: 30px;
        }

            .btn-shop:hover {
                background-color: #0056b3;
            }

        /* ====== Giới thiệu thương hiệu ====== */
        .intro-section {
            text-align: center;
            padding: 50px 20px;
        }

            .intro-section h2 {
                font-weight: 700;
                margin-bottom: 15px;
            }

            .intro-section p {
                color: #555;
                max-width: 700px;
                margin: 0 auto;
            }

        /* ====== Bộ sưu tập sản phẩm ====== */
        .product-card {
            border: none;
            border-radius: 10px;
            overflow: hidden;
            transition: all 0.3s ease;
            box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1);
        }

            .product-card:hover {
                transform: translateY(-10px);
                box-shadow: 0 6px 15px rgba(0, 0, 0, 0.2);
            }

            .product-card img {
                height: 250px;
                object-fit: cover;
            }

            .product-card .card-body {
                text-align: center;
            }

            .product-card h5 {
                font-weight: 600;
            }

        .price {
            color: #0d6efd;
            font-weight: bold;
        }

        footer {
            background: #212529;
            color: #bbb;
            padding: 25px 0;
            text-align: center;
            margin-top: 60px;
        }

            footer a {
                color: #0d6efd;
                text-decoration: none;
            }

                footer a:hover {
                    text-decoration: underline;
                }
    </style>
</head>

<body>
    <!-- ====== Header ====== -->
    <header>
        <nav class="navbar navbar-expand-lg navbar-dark">
            <div class="container">
                <a class="navbar-brand fs-4" asp-area="" asp-controller="Home" asp-action="Index">SneakerStore</a>
                <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
                    <span class="navbar-toggler-icon"></span>
                </button>

                <div class="collapse navbar-collapse" id="navbarNav">
                    <ul class="navbar-nav ms-auto">
                        <li class="nav-item"><a class="nav-link" asp-controller="Home" asp-action="Index">Trang chủ</a></li>
                        <li class="nav-item"><a class="nav-link" asp-controller="HangHoa" asp-action="Index">Sản phẩm</a></li>
                        <li class="nav-item">
                            <a class="nav-link position-relative" asp-controller="Cart" asp-action="Index">
                                🛒 Giỏ hàng
                                <span id="cart-count"
                                      class="position-absolute top-0 start-100 translate-middle badge rounded-pill bg-danger">
                                    0
                                </span>
                            </a>
                        </li>

                        <li class="nav-item"><a class="nav-link" asp-controller="Home" asp-action="Privacy">Chính sách</a></li>
                        <li class="nav-item"><a class="nav-link" href="#">Liên hệ</a></li>
                    </ul>
                </div>
            </div>
        </nav>
    </header>

    <!-- ====== Banner chính ====== -->
    <div class="container mt-4">
        <div class="hero">
            <div class="hero-content">
                <h1>Khám phá Sneaker chính hãng</h1>
                <p>Phong cách – Năng động – Thời thượng</p>
                <a href="/HangHoa/Index" class="btn btn-shop mt-3">Mua ngay</a>
            </div>
        </div>
    </div>

    <!-- ====== Giới thiệu thương hiệu ====== -->
    <div class="intro-section">
        <h2>Chào mừng đến với SneakerStore</h2>
        <p>Chúng tôi mang đến những đôi giày sneaker chất lượng nhất từ các thương hiệu nổi tiếng như Nike, Adidas, Puma và nhiều hơn nữa — giúp bạn thể hiện phong cách cá nhân mỗi ngày.</p>
    </div>
    <!-- ====== Nội dung trang con ====== -->
    <div class="container pb-5">
        <main role="main">
            @RenderBody()
        </main>
    </div>

    <!-- ====== Footer ====== -->
    <footer>
        <div class="container">
            <p>&copy; 2025 - SneakerStore | Thiết kế bởi Bootstrap 5</p>
            <p><a href="#">Facebook</a> | <a href="#">Instagram</a> | <a href="#">Liên hệ</a></p>
        </div>
    </footer>

    <script src="~/lib/jquery/dist/jquery.min.js"></script>
    <script src="~/lib/bootstrap/dist/js/bootstrap.bundle.min.js"></script>
    <script src="~/js/site.js" asp-append-version="true"></script>
    @await RenderSectionAsync("Scripts", required: false)
    <script>
        $(document).ready(function () {
            $.ajax({
                url: "/Cart/GetCartCount",
                method: "GET",
                success: function (count) {
                    $("#cart-count").text(count);
                }
            });
        });
    </script>
</body>
</html>